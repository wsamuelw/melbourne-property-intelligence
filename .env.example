# Claude API key (optional — uses local Ollama if not set)
ANTHROPIC_API_KEY=

# Ollama settings (used if no API key)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# Data sources
CRUNCHBASE_API_KEY=
